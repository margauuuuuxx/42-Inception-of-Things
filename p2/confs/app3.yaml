apiVersion: v1
kind: ConfigMap
metadata:
  name: app3-html
  labels:
    app: app3
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head><title>app3</title></head>
    <body>
      <h1>Hello from app3</h1>
    </body>
    </html>

---

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: app3
  name: app3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app3
  strategy: {}
  template:
    metadata:
      labels:
        app: app3
    spec:
      containers:
      - image: nginx
        name: nginx
        ports:
        - containerPort: 80
        resources: {}
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html/index.html
          subPath: index.html
      volumes:
      - name: html
        configMap:
          name: app3-html
status: {}

---

apiVersion: v1
kind: Service
metadata:
  labels:
    app: app3
  name: app3
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: app3
  type: ClusterIP
status:
  loadBalancer: {}
